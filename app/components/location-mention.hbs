<LinkTo @route="stories.story.locations.location" @models={{array @payload.model.story @payload.model}}>
	{{#if @payload.model.name.length}}
		{{@payload.model.name}}
	{{else}}
		{{t "location.unnamed"}}
	{{/if}}
	{{yield}}
</LinkTo>